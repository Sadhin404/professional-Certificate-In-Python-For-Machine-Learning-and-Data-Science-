import numpy as np 
array = np.array([1,2,3,45,6,7])

#calculating 
mean=np.mean(array)
median=np.median(array)
stddev=np.std(array)
varience=np.var(array)


print(mean,median,stddev,varience)

